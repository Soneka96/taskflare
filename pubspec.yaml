name: taskflare
description: A CLI tool that wraps dart test and notifies on completion.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.16.0
  path: ^1.9.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0