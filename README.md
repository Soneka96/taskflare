# taskflare
taskflare is a Dart CLI tool that wraps dart test and other commands to detect when they finish and report a single result: success, failure, or crash. It parses output, tracks execution state, and triggers notifications when the process ends.
